# next-easytour minimal example

A single-page Next.js App Router demo showing the library in basic + authoring mode.

```bash
cd examples/nextjs-app
npm install
npm run dev
```

Then visit http://localhost:3000. Click **Start tour** to walk through the four example steps, and toggle **Author mode** to drag the arrow tip and save edits.
